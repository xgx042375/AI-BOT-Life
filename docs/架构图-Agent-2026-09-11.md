# 架构图：Agent 主链与能力面（2026-09-11 · 通用化后新版 · mermaid）

> 替代口径：归档旧图 `docs/archive/agent结构图-2026-09-09.png`（顺序主链六站 + harness 层 + 三后台循环 + 数据层）。
> 新版变化：core 收敛出 **llm Provider 适配层 / packs 内容包 / special / reply / atomics / paths**；
> 出口扩为 **QQ + GAL web 双通道**；能力注册统一 **agent/registry ToolCard**。
> 配套：`docs/项目拆解.md`（模块职责）、`docs/架构图-活人感-2026-09-11.md`（活人感链路）。

PNG 渲染版：`架构图-Agent-2026-09-11.png`（重渲：提取本文件 mermaid 代码块为 .mmd 后执行 `npx @mermaid-js/mermaid-cli@11 -i <导出.mmd> -o 架构图-Agent-2026-09-11.png -b white -w 3000`；init 指令已内嵌）

```mermaid
%%{init: {"flowchart": {"useMaxWidth": false, "htmlLabels": true, "nodeSpacing": 55, "rankSpacing": 65}, "theme": "base", "themeVariables": {"fontSize": "15px", "fontFamily": "Microsoft YaHei, PingFang SC, sans-serif", "primaryColor": "#E8F0FE", "primaryBorderColor": "#5B7BD5", "lineColor": "#8A8F98", "clusterBkg": "#FFFDF0"}}}%%
flowchart LR
  QQ["QQ 客户端<br/>（NapCat 内嵌）"] --> DRV["nonebot2 driver<br/>qq-bot/harness/driver.py"]
  DRV --> HD

  subgraph CHAIN["plugins/brain 顺序主链（每条消息）"]
    HD["① brain.handle 入口<br/>plugins/brain/__init__.py<br/>纯图/声文/纯文分流 · 群聊门控 · 连发聚合"]
    PER["② 感知装配<br/>core/perception.py + memory.build_context<br/>时间/久别/点名/语义召回/挂落回放"]
    S1["③ stage1 内容生成<br/>思考自决 · 【认真】思维链<br/>think_flow 卡级优先"]
    PRE["并行任务<br/>约定自决台账 + preparse_owner_facts<br/>（主人私聊全角括号门控）"]
    S2["④ stage2 润色<br/>core/reply.generate<br/>基调目标 · 长度自决 · owner facts 注入"]
    EDGE["⑤ 硬边界兜底<br/>长度/句读/近重复/漂移回退"]
    OUT["⑥ 发送 + 消费<br/>QQ 回信 / TTS / 表情 / 约定落账"]
  end

  subgraph COREP["core 封闭核心"]
    LLM["core/llm.py Provider<br/>get_client / resolve_model<br/>thinking_extra / ctx_budget / embed_config"]
    LOCAL["本地 llama-server<br/>127.0.0.1:11434<br/>（+11435 embeddings）"]
    EXT["外部 OpenAI 兼容端点<br/>LLM_* env 一键切换"]
    SPE["core/special.py<br/>special 标记协议 · 道具目录<br/>data/special_state.json"]
    PACKS["core/packs.py robot-pack-v1<br/>双根扫描 · content/voice/world/<br/>items/tones 五索引"]
  end

  subgraph SUPP["支撑插件"]
    MEM["plugins/memory<br/>data/memory.db 全量记忆<br/>memory/embeddings.py 语义召回"]
    VOICE["plugins/voice<br/>GPT-SoVITS tts_wav<br/>EMO_PARAMS 9 类语气"]
    WG["plugins/webgal<br/>WS 单连接 token 鉴权<br/>CaptureBot 合成轮注入"]
    REG["agent/registry.py<br/>ToolCard 能力总线<br/>（livesource 等注册锚点）"]
    LS["plugins/livesource<br/>直播弹幕 → register_handler"]
  end

  subgraph WEBOUT["GAL web 出口"]
    CJ["GET /gal/content.json<br/>core.packs.content_index<br/>quotes 分卡 + spriteMap"]
    TTSAPI["POST /gal/tts<br/>逐行语音（tone 同源基调）"]
    GH["data/gal_history.jsonl<br/>回想轮次落盘"]
    GAL["launcher/web/gal.html + gal.js<br/>GAL 客户端 · GAL_RENDERER 钩子"]
  end

  PER --> S1 --> S2 --> EDGE --> OUT
  HD --> PER
  PRE -.->|stage2 组装时收取| S2
  S1 -->|"ctx_budget 预算 · 思维链开关"| LLM
  S2 --> LLM
  LLM --> LOCAL
  LLM --> EXT
  PER --> MEM
  MEM -->|"facts/events/summaries"| DB[("data/memory.db")]
  MEM --> EMB["memory/embeddings.py<br/>:11435 向量"]
  SPE -->|感知注入 + obey 切片| S2
  OUT -->|【基调】登记| VOICE
  PACKS -->|persona 回落 · quotes · 差分词| PER
  PACKS --> CJ
  PACKS -->|voice.json setdefault| VOICE
  PACKS -->|items 目录| SPE
  OUT -.->|gal 模式停用 QQ 通道| WG
  WG <-->|"WS 帧 auth/seg/sprite/state"| GAL
  WG --> CJ
  WG --> TTSAPI
  TTSAPI --> VOICE
  WG --> GH
  GAL -->|注入合成轮| HD
  LS --> REG
  REG -.->|弹幕钩子| HD
```

## 读图要点

1. **顺序主链六站**与旧图同构（入口→感知→stage1→stage2→兜底→发送消费），新增并行任务泳道：
   约定自决与 `preparse_owner_facts` 与 stage1 同时 `create_task`，stage2 组装时收取。
2. **所有 LLM 调用走 `core/llm.py`**：`get_client(purpose)` 单例、`resolve_model(purpose)` 专用模型键、
   `thinking_extra` 思维链参数（仅本地 provider 注入）、`ctx_budget` 上下文预算；
   本地引擎默认，`LLM_*` env 切外部端点、代码零改动。
3. **packs 是内容面单入口**：persona 回落（包卡）、`/gal/content.json` 台词与差分映射、
   voice 合并、道具目录，全部"无包=原行为"。
4. **registry（ToolCard）是代码级扩展唯一正路**（铁律 1）：livesource 弹幕钩子经它注册，
   pack type=tool 装载后同样落到 ToolCard 契约。
5. **双出口**：QQ 通道与 GAL web 通道互斥（webgal gal 门停用 QQ 通道）；GAL 页文字逐行
   `POST /gal/tts` 取语音，sprite 帧切差分，回想轮次落 `data/gal_history.jsonl`。
