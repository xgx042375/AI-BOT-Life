# 部署指南（装、升级、搬走、卸掉）

> **本文怎么用**：第一次装看 §1；想知道"哪个功能要装什么"看 §2；换电脑/换盘看 §4。
> 运维与排错见 `维护手册.md`；做 mod 见 `MOD开发指南.md`。
> **一句话前提**：**没有任何外部组件是必需的**。最小形态是"Python + 这个仓"，
> 它能跑纯文本/视觉小说模式；模型、QQ、语音、Live2D 全是可选件。

---

## 0. 三种部署形态（先选一个）

| 形态 | 你需要什么 | 得到什么 | 体积量级 |
|---|---|---|---|
| **最小（纯文本/GAL）** | Python 3.13 + venv 依赖 | GAL 页/网页模式对话；模型走外部 API（填 `LLM_BASE_URL`） | 约 170 MB |
| **标准（本地模型）** | 最小 + llama.cpp + Gemma 权重 | 离线可用；加上 embedding 后记忆语义检索 | 约 9 GB |
| **完整（QQ + 语音）** | 标准 + NapCat + GPT-SoVITS + 音色权重 | QQ 里收发消息、发语音 | 约 47 GB |

> ⚠️ **NapCat 与 GPT-SoVITS 都不能由本框架代分发**：NapCat 内含腾讯 QQ 客户端组件（再分发受限），
> GPT-SoVITS 的整合包与音色权重体积大且授权在用户侧。**本框架只检测 + 指路，不自动下载。**

---

## 0.5 懒人包（自己打包分发 · **只带可执行框架**）

要给朋友发"解压即用"的版本，别手压整个目录——用打包器，它**强制排除**数据库、运行状态、凭据与第三方素材：

```powershell
cd qq-bot
.venv\Scripts\python.exe tools\dev\make_lazybundle.py --tier framework --write D:\qqai-lazy
```

| 档位 | 体积 | 内容 |
|---|---|---|
| **`framework`（默认）** | **约 7.5 MB** | 源码 + 启动器 exe + 文档 + 示例包 + 包内 `安装.ps1`（一键建 venv/装依赖）与 `懒人包说明.md` |
| `standard` | ≈ 11 GB | + llama.cpp 与本地模型权重 |
| `full` | ≈ 48 GB | + GPT-SoVITS 与音色权重 |
| `--with-wheels` | + 2.4 GB | torch 缓存（只在做模型转换 / LoRA 训练时需要） |
| `--with-private` / `--with-restricted` | — | 带上第三方素材（角色卡/方舟皮肤/表情包）/ NapCat —— ⚠️ **只可私下传，不可公开发布** |

**永不入包**（任何档位、任何开关都不豁免）：`data\memory.db`、`agent_checkpoints.db`、`data\*.json` 运行状态、
`.env` 凭据、`qq-bot\.venv`（内含绝对路径，换机必失效）、日志、`vault\`。

> 收包的人需要自备两样：**Python 3.13**，以及**一个模型或一个 API Key**（外部 API 是设计内的正路，不算降级）。
> 对外**公开发布**请用官方发行包（SDK / Core / 启动器三件，见仓库 Releases），不要发懒人包。

---

## 1. 全新安装

```powershell
# ① 放好目录（本指南以 <安装根> 指代这个目录）
#    把仓库解压/克隆到任意路径，例如 E:\robot —— 路径不再被硬编码（2026-09-12 起由 core/paths.py 推导）

# ② 建 Python 环境
cd <安装根>\qq-bot
python -m venv .venv
.venv\Scripts\python.exe -m pip install -U pip
.venv\Scripts\python.exe -m pip install -e .        # 依赖清单见 pyproject.toml

# ③ 配置身份（**必做**，否则 bot 起不来）
copy .env.example .env
notepad .env
#   SUPERUSERS=["你的QQ号"]      ← 必须是 JSON 数组（裸数字会让 bot 起不来，见维护手册 §4.1）
#   OWNER_NICKNAME=你的昵称       ← 不填会回落本机默认串

# ④ 启动（会自动按需拉起引擎/NapCat）
cd <安装根>
.\start.bat
```

**验证装好了**：

```powershell
<安装根>\launcher\Launcher.ps1 -Mode status     # 或直接看启动器「运行状态」页
```

四灯（引擎 / 记忆 / QQ / 机器人）里**只有「机器人」是必须亮的**——其余三盏灭着，项目依然可用。

---

## 2. 功能 → 依赖矩阵

<!-- BEGIN GENERATED: deps-matrix -->

<!-- 自动生成，勿手改。源: launcher/deps.json（updated=2026-09-12） · 重新生成: qq-bot/tools/dev/sync_deps_doc.py --write -->

| 功能 | 必需 | 检测方式 | 缺失后果 |
|---|---|---|---|
| 机器人本体 | **必须** | `TCP 8080` | 不存在替代路径（没有它就没有这个项目） |
| 本地推理引擎（对话模型） | 可选 | `TCP 11434 + 进程 llama-server` | 改为连外部 API：启动器「模型与接口」里选任一家预设或填自定义端点（LLM_BASE_URL / LLM_API_KEY / LLM_MODEL）。**这是设计内的正路，不是降级** |
| 记忆向量检索（embedding） | 可选 | `TCP 11435` | 记忆回落到关键词匹配：仍能记住与回忆，只是『意思相近』的联想变弱 |
| QQ 通道（协议端） | 可选 | `进程 NapCat + TCP 8080 可连` | 无 QQ 通道：仍可用 GAL 页/网页模式对话（纯文本与视觉小说界面）。**上架发行以 GAL 为主界面即为此设计** |
| 语音合成 | 可选 | `目录存在` | 只发文字。启动器「启动语音」开关会自动保持关闭 |
| 直播弹幕投放 | 可选 | `.env LIVE_DANMAKU_ENABLED 且 blivedm 已装` | 该模块整块不加载（零副作用）。缺依赖时自动保持禁用并留一行日志 |
| Live2D 演出（VTube Studio） | 可选 | `.env VTS_ENABLED 且 VTS 在听 8001` | 无 Live2D 演出；表情仍以图片表情包形式发送 |
| 启动器界面（内嵌浏览器） | 可选 | `目录存在` | 启动器仍是可用窗口（WPF 原生页：设置/干员/日志/状态/关于），但主页与 GAL 页不可渲染 |
| 启动器本体 | 可选 | `exe 存在` | 用命令行：start.bat 启动、stop.ps1 停止、qq-bot\.env 手工改配置 |

**读法**：只有第一行是「必须」——**没有任何外部组件是必需的**。其余每一行拿掉之后，都有一条明确写着它怎么降级的路径。

| 功能 | 组件 | 本机路径 | 本机体积 | 上游来源 | 获取方式 |
|---|---|---|---|---|---|
| 机器人本体 | Python 3.13 + venv 依赖 | `qq-bot/.venv` | 167 MB | [https://www.python.org/downloads/](https://www.python.org/downloads/) | python -m venv qq-bot/.venv  然后  qq-bot\.venv\Scripts\pip install -e qq-bot（或按 pyproject.toml 装依赖） |
| 本地推理引擎（对话模型） | llama.cpp 运行时（llama-server.exe） | `tools/llama.cpp` | 1,129 MB | [https://github.com/ggml-org/llama.cpp/releases](https://github.com/ggml-org/llama.cpp/releases) | 解压上游 win-cuda 版到 tools\llama.cpp\（本机现用版本见维护手册 §1.3；另有 tools\llama-new\ 备用副本，未启用） |
| 本地推理引擎（对话模型） | 对话模型权重（.gguf） | `data/models/gemma4-12b` | 7,039 MB | 上游模型卡（再分发条款须自行确认） ⚠️**待核** | 放入 data\models\gemma4-12b\；文件名须与 plugins/brain 的 MODELS 表一致（现为 gemma-4-12B-it-heretic-Q4_K_M.gguf） |
| 记忆向量检索（embedding） | embedding 模型权重 | `data/models/embedding` | 610 MB | 上游模型卡（Qwen3-Embedding-0.6B） ⚠️**待核** | 放入 data\models\embedding\Qwen3-Embedding-0.6B-Q8_0.gguf |
| QQ 通道（协议端） | NapCat（NTQQ 协议端） | `tools/napcat` | 325 MB | [https://github.com/NapNeko/NapCatQQ](https://github.com/NapNeko/NapCatQQ) | 解压到 tools\napcat\；**本框架不自动下载也不代为分发**（内含腾讯 QQ 客户端组件，不可再分发）。安装与 OneBot 连接配置见 docs\部署指南.md §2.5 |
| 语音合成 | GPT-SoVITS 整合包 | `tools/GPT-SoVITS-v4-package` | 37,830 MB | [https://github.com/RVC-Boss/GPT-SoVITS](https://github.com/RVC-Boss/GPT-SoVITS) | 解压整合包到 tools\GPT-SoVITS-v4-package\；**权重不随本框架分发**，用户自备 |
| 语音合成 | 音色权重（.ckpt/.pth + 参考音） | `data/voices` | 316 MB | 自训练/自备 | 放进 data\voices\<音色键>\；具体路径写进包 voice.json 或 plugins/voice 的 VOICES 表 |
| 直播弹幕投放 | blivedm（Python 包） | `qq-bot/.venv` | 1 MB | PyPI | qq-bot\.venv\Scripts\pip install blivedm |
| Live2D 演出（VTube Studio） | VTube Studio（Steam 应用） | `（外部程序，非本安装根）` | （外部程序） | Steam 商店（付费/免费视版本） | 装好后在 VTS 里启用 API（默认端口 8001），再在 .env 打开 VTS_ENABLED=true |
| 启动器界面（内嵌浏览器） | Microsoft WebView2 运行时 | `launcher/webview2` | 101 MB | [https://developer.microsoft.com/microsoft-edge/webview2/](https://developer.microsoft.com/microsoft-edge/webview2/) | Win11 通常已内置 Evergreen 运行时；本项目另带固定版（launcher\webview2\）以免受系统版本波动影响 |
| 启动器本体 | QQAI-Launcher.exe（ps2exe 产物） | `launcher/QQAI-Launcher.exe` | 1 MB | 本仓源码构建（launcher\README.md §8） | 无需安装；源码改了要重编译（ps2exe，需 -STA -x64 -requireAdmin，见 README §8） |

> 本机实测总体积合计约 **46.4 GB**（不含外部程序）。体积大的是模型与语音运行环境，都是**可选的**。
<!-- END GENERATED: deps-matrix -->

> 本表的**唯一事实源**是 `launcher/deps.json`（不是本文）。启动器做能力检测时读的也是它。
> 改依赖关系请改 JSON，再跑 `qq-bot\tools\dev\sync_deps_doc.py --write` 重新生成本表——
> 不一致时 `audit_consistency.py` 会报出来（**同一事实两处写必然漂移**，这个项目吃过太多次）。

---

## 2.5 QQ 通道接入（可选 · NapCat）

> **本框架不代分发、不自动下载 NapCat**——它内含腾讯 QQ 客户端组件，再分发受限。
> 上游：`https://github.com/NapNeko/NapCatQQ`（混合许可，见 `THIRD_PARTY.md`）。
> 本节内容原先只写在 `<安装根>\tools\NapCat对接说明.md`——那份**在 gitignored 的 `tools\` 下，
> 不随发行物分发**，故内联到此（照原样，只去掉硬编码盘符）。

**① 解压运行**

1. 把上游 NapCat 包解压到 `<安装根>\tools\napcat\`
2. 运行其中的 `napcat.exe`（或 `launcher.bat`）
3. **用机器人 QQ 小号扫码登录**（唯一必须人工参与的步骤）

> NapCat 5.x 启动后可在浏览器访问 `http://127.0.0.1:6099/webui`（WebUI 管理）；
> 登录后会生成 `napcat/config/onebot11_<QQ号>.json`。

**② 配置 OneBot v11（在 WebUI 改，或直接编辑那个 json）**

```json
{
  "network": {
    "enable": true, "name": "qq-bot", "type": "websocket_server",
    "host": "127.0.0.1", "port": 8080,
    "messagePostFormat": "array", "enableHeartbeat": true
  },
  "http": { "enable": false },
  "httpPost": { "enable": false }
}
```

即：**NapCat 作为 `websocket_server` 监听 `127.0.0.1:8080`，bot 反向连接到 `ws://127.0.0.1:8080/onebot/v11/ws`**
（与 bot 的 HTTP 同端口——这是 nonebot 反向连接的默认端口）。

**③ 验证**

- bot 日志出现 `WebSocket reverse connection ... connected` 即打通
- 私聊机器人发任意消息 → 应有回复；群里 @机器人 → 应有回复

**④ 风险提示**（照原样保留，这不是免责套话）

- 非官方协议：**请用小号、控制频率**
- 若被封：换号 + 降低触发频率；本项目配置与数据分离，换号只需重新登录 NapCat（不必重装）

---

## 3. 升级

| 情形 | 做法 | 会丢什么 |
|---|---|---|
| 框架更新（覆盖 `qq-bot/`） | 用新发行版覆盖 `qq-bot\`，**保留** `data\`、`qq-bot\data\`、`.env` | 无（数据全在 `data\`） |
| 启动器更新 | 替换 `QQAI-Launcher.exe`（**两处**：`<安装根>\` 与 `<安装根>\launcher\`） | 无 |
| 数据包/皮肤更新 | 直接覆盖 `data\packs\` / `launcher\web\skins\` 下对应目录 | 无 |

**升级前**（涉及数据的手术，如 uid 迁移、库清洗）：
```powershell
copy <安装根>\data\memory.db <安装根>\vault\backups\memory.db.bak_before_<事由>_<日期>
```

> 目录约定保证升级不丢东西：`qq-bot/plugins/`（框架，会被覆盖）· `data/packs/`（内容包，永不覆盖）·
> `data/plugins/`（第三方插件，永不覆盖）· `launcher/web/skins/`（皮肤，永不覆盖）。

---

## 4. 换盘 / 换机器（迁移）

1. **整个目录拷走**（`data\`、`qq-bot\.venv\` 都带上；`tools\` 太大可单独拷）。
2. **venv 必须重建**（里面记着绝对路径）：
   ```powershell
   cd <新安装根>\qq-bot
   rmdir /s /q .venv
   python -m venv .venv && .venv\Scripts\python.exe -m pip install -e .
   ```
3. **确认路径推导**：
   ```powershell
   <新安装根>\launcher\Launcher.ps1 -Mode status
   ```
   能打印四灯与显存即路径正常（2026-09-12 起核心路径由 `core\paths.py` 相对推导，不再写死 `E:\robot`）。
4. **重编启动器 exe**（可选，仅当你想从新路径双击 exe）：`launcher\README.md` §8。

**已知残留**（会因换盘失效，见 `遗留任务.md` C1）：
`qq-bot\plugins\voice\**` 里仍有几处硬编码 `E:\robot\...` → 换盘后语音会失效，其余功能正常。

---

## 5. 卸载

本框架**不写注册表、不写系统目录、不装服务**——卸载 = 删目录。

1. 先停：`stop.ps1` 或启动器「停止」（确认没有 `llama-server` / `python` / `NapCat` 残留进程）。
2. 想留记忆就备份 `data\memory.db`，想留人设就备份 `qq-bot\data\personas\`、`data\*.json`。
3. 删掉安装根目录。
4. 可选清理：VTube Studio（若为它装过）、WebView2 运行时（Win11 自带，不必删）。

---

## 6. 装不起来的排查顺序

| 症状 | 先查 |
|---|---|
| 双击 `start.bat` 一闪而过 | 在该目录开 PowerShell 跑 `.\start.ps1`，看红字 |
| bot 起不来、报 pydantic 校验失败 | `.env` 的 `SUPERUSERS` 不是 JSON 数组（见维护手册 §4.1） |
| 8080 起不来 | 端口被占：`netstat -ano \| findstr :8080` |
| 引擎起不来 | `data\llama-server.err.log`；显存是否被占；模型文件是否在 `data\models\` |
| 启动器白屏 | 看 `launcher\webstep.log`；WebView2 运行时是否完整 |
| 语音不出声 | GPT-SoVITS 目录与权重路径；`data\voice_mode.json` 开关 |
| 记不住事 | `data\memory.db` 是否存在且非空；embedding 灯灭着也能用（降级为关键词） |

更全的故障表见 `维护手册.md` §7.1。
