# 架构图：活人感链路（2026-09-11 · 通用化后新版 · mermaid）

> 对齐口径：归档旧图 `docs/archive/活人感拆解图-2026-09-09.png`（主链六站活人感能力 + 后台循环喂养 +
> 记忆沉淀 + 自决判定）。
> 新版聚焦"活人感"的**状态生产-消费闭环**：lifesim 心跳 → 心情/special 状态 → 主链感知注入 →
> 呈现面（状态页时间线 / GAL 回想）。
> 配套：`docs/架构图-Agent-2026-09-11.md`（主链全貌）、`docs/项目拆解.md` §四。

PNG 渲染版：`架构图-活人感-2026-09-11.png`（重渲：提取本文件 mermaid 代码块为 .mmd 后执行 `npx @mermaid-js/mermaid-cli@11 -i <导出.mmd> -o 架构图-活人感-2026-09-11.png -b white -w 3000`；init 指令已内嵌）

```mermaid
%%{init: {"flowchart": {"useMaxWidth": false, "htmlLabels": true, "nodeSpacing": 55, "rankSpacing": 65}, "theme": "base", "themeVariables": {"fontSize": "15px", "fontFamily": "Microsoft YaHei, PingFang SC, sans-serif", "primaryColor": "#E8F0FE", "primaryBorderColor": "#5B7BD5", "lineColor": "#8A8F98", "clusterBkg": "#FFFDF0"}}}%%
flowchart TB
  subgraph BEAT["agent/lifesim.py 心跳（自决节奏 · 与用户活跃度解耦）"]
    LOOP["lifesim_loop<br/>启动恢复 tick（距上次≥5分钟才补）<br/>wake_min 自决 ±10% 抖动"]
    TICK["lifesim_tick<br/>轨迹回看 _recent_trace 防循环<br/>夜间睡眠保护 · 久别大于2小时注入这段的事"]
    DOING["doing 生成<br/>max_tokens=120（2026-09-11 半句截断修复）<br/>temperature=0.9"]
    LSTATE[("data/life_state.json<br/>doing/plan/scene/mood/约定痕迹")]
    LLOG[("data/life_log.jsonl<br/>LIFE_LOG_MAX=400（时间线约8天翻阅量）")]
    DIARY["data/life_diary.jsonl 每日日记<br/>write_daily_diary"]
  end

  subgraph MOODC["心情链路（全局一份，跨会话同一份）"]
    MD["core/mood.py apply<br/>① agent 自标【心情】标记（reply 剥除）<br/>② 心跳向平静漂移 _mood_drift"]
  end

  subgraph SPEC["special 状态（agent 自决 · 机器只记账）"]
    SM["core/special.py<br/>apply_agent_markers / apply_owner_facts<br/>登记-解除-TTL 到期自动失效"]
    SST[("data/special_state.json<br/>标记/道具/情绪层账本")]
    SPTXT["special.perception_text<br/>感知注入文本"]
  end

  subgraph CONV["对话侧喂养"]
    BR["plugins/brain 主链<br/>stage2 基调自标 · 约定自决台账"]
    MICRO["lifesim.micro_update_from_conversation<br/>apply_interaction 对话介入修正"]
    EMO["plugins/emotion classify_emotion<br/>9类情绪 → VTube Studio Live2D 注入<br/>失败规则兜底 · 落库 emotions"]
    CORR["plugins/correction 矫正<br/>data/corrections.json 注入说话方式"]
  end

  subgraph GATE["主动消息门控（plugins/debug 后台循环）"]
    PL["proactive 循环（45分钟级）<br/>候选=主人白名单 · 未回复不再扫描"]
    PD["感知后 agent 自决说不说<br/>发送成功才落账（防复读）"]
    PST[("data/proactive_state.json<br/>proactive_state 账本")]
  end

  subgraph SHOW["时间线 / 回想呈现链"]
    INJ["主链感知注入<br/>lifesim.life_text 此刻的你<br/>+ special 感知文本 + 心情"]
    SPAGE["启动器状态页<br/>launcher/Launcher.ps1 Get-LifeLog 尾100条<br/>ScrollViewer 滚动翻阅"]
    WGH["plugins/webgal read_gal_history<br/>GAL 回想轮次"]
    GALJ["launcher/web/gal.js 回想渲染<br/>data/gal_history.jsonl"]
  end

  LOOP --> TICK --> DOING
  DOING --> LSTATE
  TICK --> LLOG
  TICK --> DIARY
  LSTATE --> MD
  TICK -->|心情漂移| MD
  BR -->|自标标记| MD
  BR -->|自决标记/owner facts| SM
  SM --> SST
  SM --> SPTXT
  BR --> MICRO
  BR --> EMO
  BR --> CORR
  PL --> PD --> PST
  LSTATE -.->|状态素材| PL
  LSTATE --> INJ
  LLOG -->|这段的事/轨迹| INJ
  SPTXT --> INJ
  MD -->|mood 字段| INJ
  MICRO -->|即时落状态| LSTATE
  LLOG -->|尾100条| SPAGE
  LSTATE --> SPAGE
  GH2[("data/gal_history.jsonl")] --> WGH --> GALJ
```

## 读图要点

1. **心跳是活人感的"生活"来源**：与用户活跃度解耦（用户不在线也照常转）；doing 生成
   `max_tokens=120`（长度感归提示层与前端句读截断），状态进 `life_state.json`、事件进
   `life_log.jsonl`（上限 400 条，配合状态页 ScrollViewer 滚动翻阅约 8 天）。
2. **心情链路两通道**：agent 自标【心情】（零额外调用，`core/reply` 剥除标记）+ 心跳"向平静漂移"
   自然衰减；存 `life_state.json`，经 `life_text` 注入"此刻的你"。
3. **special 状态 = agent 自决标记**（登记/解除/TTL 到期失效），机器只存事实不代写演出；
   道具目录来自内容包（type=item）+ 开放集登记。
4. **主动消息全链自决**：找不找 TA / 什么时候找 / 说什么全 agent 决定；门控硬纪律只有
   "候选白名单 + 未回复不再扫描 + 发送成功才落账"。
5. **呈现闭环**：状态页时间线（尾 100 条滚动翻阅）与 GAL 回想（`gal_history.jsonl`）消费同一份
   生活状态；久别重逢时 `life_log` 作为「这段时间的事」回注主链——沉淀即闭环。
